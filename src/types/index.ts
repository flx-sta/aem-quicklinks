export interface DomainConfig {
  regex: string;
  domains: string[];
}

export interface LinkUrls {
  preview?: string;
  editor?: string;
  properties?: string;
  crxde?: string;
  sites?: string;
}
